import {
    Typography
} from "@mui/material";
import { Card } from "../../components";

export const Work = () => {
    return (
        <div className="work">
            <Card />
        </div>
    )
}