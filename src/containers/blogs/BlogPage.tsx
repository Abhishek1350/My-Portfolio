import {
    Typography
} from "@mui/material";
import styles from "./style.module.css"
import { BlogCard } from "../../components";

export const BlogPage = () => {
    return (
        <div className="Blogs">
            {/* <BlogCard/> */}
        </div>
    )
}