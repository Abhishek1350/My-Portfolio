import { Container } from "@mantine/core"

export default function ContactPage() {
    return (
        <main>
            <Container size="lg">
                Contact Page
            </Container>
        </main>
    );
}